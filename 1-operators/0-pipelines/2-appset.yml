---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: tkn-operator
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: tkn-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: openshift-pipelines-{{name}}
    spec:
      project: ansible-automation-platform
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/mmayeras/gitops-demo.git
        targetRevision: main
        path: XaC/6-tkn
      destination:
        server: "{{server}}"